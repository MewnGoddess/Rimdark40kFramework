using Verse;

namespace Core40k;

public class CompProperties_AlternateTexture : CompProperties
{
    public CompProperties_AlternateTexture()
    {
        compClass = typeof(CompAlternateTexture);
    }
}